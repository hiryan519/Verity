# AGENTS.md

本文档约束后续 AI Agent 在本仓库中协作开发 AI Product Research Agent Console 时的行为。

## 1. 上下文阅读策略

首次进入本项目、用户明确更新产品决策、进入新的开发阶段、或任务会影响多处核心机制时，按顺序完整阅读：

1. `docs/AI_Product_Research_Agent_PRD.md`
2. `docs/PRD.md`
3. `docs/ARCHITECTURE.md`
4. `docs/TODO.md`
5. `docs/DECISION_LOG.md`
6. 本文件 `AGENTS.md`

日常开发任务不要求每次重复全文阅读所有文档，但必须：

- 完整阅读本文件 `AGENTS.md` 与 `docs/DECISION_LOG.md`。
- 根据任务范围读取相关 PRD、架构或 TODO 章节。
- 在动手前检查相关文档是否发生变化。
- 如果发现文档冲突、需求跨越多个核心机制、或上下文不足，应回到上述完整阅读流程。

如果用户后续提供 Gemini 生成的设计规范或其他正式设计产物，再将其作为 UI 开发的必读上下文。

## 2. 项目目标

本项目目标不是重写 Evolva，也不是做一个普通竞品分析报告生成器。

本项目是在 Evolva Agent Infra 之上，构建一个面向 AI 产品经理的竞品研究 Agent 工作台，重点展示：

- 任务理解。
- 专家 Agent 编排。
- 证据采集。
- 证据可信度。
- Analysis Pack。
- QA Gate。
- 报告生成。
- 引用追溯。
- 决策链路 Trace。
- 知识沉淀。
- Research Memory。

## 3. 产品决策优先级

当代码实现与产品决策冲突时，优先级如下：

1. 用户最新明确指令。
2. `docs/DECISION_LOG.md`。
3. `docs/AI_Product_Research_Agent_PRD.md`。
4. `docs/PRD.md`。
5. `docs/ARCHITECTURE.md`。
6. 当前代码实现。

如果发现文档之间冲突，不要自行猜测，应指出冲突并等待确认，或在影响很小的情况下先标 `待确认`。

## 4. 开发原则

- 做最小可验证实现。
- 不新增未被请求的复杂功能。
- 不把 mock 能力包装成真实能力。
- 不为了展示效果绕过产品边界。
- 每个核心模块都要能解释输入、输出、风险和验收标准。
- 修改现有代码时保持手术式变更。
- 不重构无关代码。

## 5. 文档维护最小化原则

- 不因普通代码改动同步所有文档。
- 只更新与本次任务直接相关的一个主文档。
- 只有改变产品方向、技术选型、核心机制边界时，才更新 `docs/DECISION_LOG.md`。
- `docs/TODO.md` 只记录任务状态和下一步，不承载详细设计。
- 机制细节放 `docs/MECHANISM.md`。
- 架构细节放 `docs/ARCHITECTURE.md`。
- 视觉规范放 `docs/DESIGN.md`。
- 普通代码实现通常不改文档，除非发现现有规则不足、过期或冲突。

## 6. 明确禁止

禁止：

- 把“绕过反爬”写成产品能力。
- 宣称已完成全网舆情采集，除非有真实、合规、可复查数据。
- 用纯 LLM 主观判断替代证据可信度规则。
- 生成没有数据表支撑的图表。
- 让低置信证据支撑强结论。
- 让 QA Gate 无限返工。
- 把历史报告、知识库、证据库存储直接等同于 Agent Memory。
- 修改 Evolva 底层核心逻辑来服务单个页面展示，除非用户明确确认。
- 在未脱敏情况下展示密钥、Cookie、API Key 或敏感输入。

## 7. Mock 数据规则

允许使用 mock 数据，但必须遵守：

- UI 阶段可以使用 mock。
- mock 数据文件或接口命名必须能看出是 mock。
- 页面不要暗示 mock 数据来自真实抓取。
- 后续接入真实 workflow 时，应逐步替换 mock 数据源。

## 8. 证据规则

证据必须尽量包含：

- 标题。
- URL 或文件来源。
- 来源类型。
- 平台。
- 抓取时间。
- 摘要。
- 可信度总分。
- 风险提示。

证据可信度必须基于多维度规则：

- 来源权威性。
- 内容相关性。
- 可复查性。
- 时效性。
- 信息具体性。
- 多源支持。
- 风险扣分。

如果某条证据无法打开、抓取失败、来源不明或样本不足，应显式标注。

## 9. Claim 与报告规则

核心结论必须尽量绑定证据。

结论状态建议使用：

- supported。
- weakly_supported。
- conflicted。
- unresolved。
- unsupported。

Report Writer 不应新增 Analysis Pack 中不存在、且未绑定证据的核心结论。

如果必须写推断，应标注为推断或假设。

## 10. QA Gate 规则

QA Gate 检查的是报告前的 Analysis Pack。

必须输出：

- pass / rework。
- 维度分数。
- 问题。
- 改进建议。
- 返工次数。

返工最多 1 次。

返工后仍不通过，应生成带风险提示的报告，或请求人工确认。

## 11. Trace 规则

任何真实 Agent Workflow 都应尽量记录 Trace。

TraceStep 至少包含：

- 阶段。
- 专家。
- 任务。
- 模型。
- Prompt。
- Input。
- Output。
- Token。
- 耗时。
- 状态。
- 关联证据。
- 关联报告章节。

展示前必须脱敏敏感信息。

## 12. Research Memory 规则

知识库、历史报告、证据库不是 Memory。

Memory 必须影响后续 Agent 行为。

可进入 memory candidate 的内容：

- 用户明确要求记住。
- 用户标注为认同或亮点。
- 通过质检的高置信结论。
- 多次重复出现的研究模式。
- 专家返工中沉淀出的 lesson。

不应自动进入 active memory 的内容：

- 低置信结论。
- 冲突未解决的判断。
- 单一来源强推断。
- 临时上下文。
- 敏感信息。
- 过期或不可复查信息。

## 13. 页面开发规则

页面清单和布局以 `docs/AI_Product_Research_Agent_PRD.md` 第 6 节为准。

UI 开发应读取 `docs/DESIGN.md`。后续如用户提供新的 Gemini 设计产物或明确调整视觉方向，应先更新设计规范，再进入页面实现。

UI 开发时：

- 不把首页做复杂。
- 首页只负责发起调研。
- 复杂配置放在调研范围确认页。
- 报告页优先保证三栏布局、引用追溯、证据侧栏。
- 决策链路页优先保证 Trace 可读、可筛选、可展开。

## 14. 验收习惯

每完成一个阶段，至少验证：

- 页面是否能打开。
- 核心交互是否可用。
- 数据是否来自预期来源。
- mock 与真实能力是否区分清楚。
- Trace / 证据 / QA / Memory 是否没有被静态假数据冒充。

如果无法运行测试或启动服务，必须在最终回复中说明。
